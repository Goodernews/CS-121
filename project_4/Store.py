import numpy as np

def buy_at(price_seed): # buy prices
    np.random.seed(seed=price_seed) #fixes prices for the round
    pass
def sell_prices(price_seed): # changes base prices based on the round number
    np.random.seed(seed=price_seed)
    pass
def display( price_seed):
  print("Welcome to the store! Prices change every ")
  pass #Sell only items

def sell(character, parsed, price_seed):
  pass
  
def buy(character, parsed, price_seed):
  pass